version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Installing dependencies
      - npm install
  build:
    commands:
      - echo Build step - no build steps needed for this simple app
artifacts:
  files:
    - '**/*'
